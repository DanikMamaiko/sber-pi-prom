{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  namespace: {{ .Release.Namespace | quote }}
  name: {{ include "sberpi.serviceAccountName" . }}
  labels:
    {{- include "sberpi.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
