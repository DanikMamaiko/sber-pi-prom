SberPI release {{ .Release.Name }} was installed into namespace {{ .Release.Namespace }}.

Check the rollout:
  kubectl -n {{ .Release.Namespace }} get pods,svc,ingress
  kubectl -n {{ .Release.Namespace }} rollout status deployment/{{ include "sberpi.fullname" . }}-api
  kubectl -n {{ .Release.Namespace }} rollout status deployment/{{ include "sberpi.fullname" . }}-frontend

If a migration hook fails:
  kubectl -n {{ .Release.Namespace }} logs job/{{ include "sberpi.fullname" . }}-migrate
