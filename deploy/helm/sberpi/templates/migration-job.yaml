{{- if .Values.migrations.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  namespace: {{ .Release.Namespace | quote }}
  name: {{ include "sberpi.fullname" . }}-migrate
  labels:
    {{- include "sberpi.labels" . | nindent 4 }}
    app.kubernetes.io/component: migration
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  backoffLimit: {{ .Values.migrations.backoffLimit }}
  activeDeadlineSeconds: {{ .Values.migrations.activeDeadlineSeconds }}
  template:
    metadata:
      labels:
        {{- include "sberpi.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: migration
    spec:
      restartPolicy: Never
      automountServiceAccountToken: false
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: migrate
          image: {{ include "sberpi.image" .Values.api.image | quote }}
          imagePullPolicy: {{ .Values.api.image.pullPolicy }}
          command: ["sh", "-c"]
          args:
            - >-
              set -eu;
              if [ "${AUDIT_ENABLED:-true}" = "true" ]; then
                alembic -c audit_alembic.ini upgrade head;
              fi;
              alembic upgrade head
          env:
            - name: APP_ENV
              value: {{ .Values.config.appEnv | quote }}
            - name: AUTH_PROVIDER
              value: {{ .Values.config.authProvider | quote }}
            {{- if include "sberpi.secretFiles" . }}
            - name: SBERPI_SECRETS_DIR
              value: /run/secrets/sberpi
            {{- end }}
            - name: AUDIT_ENABLED
              value: {{ .Values.config.auditEnabled | quote }}
          {{- if not (include "sberpi.secretFiles" .) }}
          envFrom:
            - secretRef:
                name: {{ required "secrets.existingSecret is required" .Values.secrets.existingSecret }}
          {{- end }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          volumeMounts:
            {{- if include "sberpi.secretFiles" . }}
            - name: credentials
              mountPath: /run/secrets/sberpi
              readOnly: true
            {{- end }}
            - name: tmp
              mountPath: /tmp
      volumes:
        {{- if include "sberpi.secretFiles" . }}
        - name: credentials
          secret:
            secretName: {{ required "secrets.existingSecret is required" .Values.secrets.existingSecret }}
            defaultMode: 0440
        {{- end }}
        - name: tmp
          emptyDir: {}
{{- end }}
